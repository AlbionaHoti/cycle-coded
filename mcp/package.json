{
  "name": "cycle-coded-mcp",
  "version": "0.1.0",
  "description": "Local-first MCP server for cycle-coded mode state (period phase + eras + astro flags)",
  "type": "module",
  "bin": {
    "cycle-coded-mcp": "./server.mjs"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
